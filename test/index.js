import {expect} from 'chai';
import {route, match} from '../';


describe('Rondpoint', () => {
    describe('route', () => {
        it('should accept a string or regexp', () => {

        });
    });

    describe('match', () => {
        it('should accept a string and a routes array', () => {

        });
    });
});
